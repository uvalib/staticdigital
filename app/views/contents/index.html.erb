<p id="notice"><%= notice %></p>

<h1>Contents</h1>

<table>
  <thead>
    <tr>
      <th>Link</th>
      <th>UVA-only</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @contents.each do |content| %>
      <tr>
        <% url = File.join(ENV['CONTENT_DOWNLOAD_URL'], content[:"id"]) %>
        <td><%= link_to(url, url) %></td>
        <td><%= content[:"uva-only"] %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

<b><%= link_to 'New Content', new_content_path %> </b>


