<%= form_with(model: staticdc, multipart: true) do |form| %>
  <% if staticdc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staticdc.errors.count, "error") %> prohibited this staticdc from being saved:</h2>

      <ul>
      <% staticdc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :public %>
    <%= form.check_box :public %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address %>
  </div>

  <div class="field">
    <%= form.label :pdf %>
    <%= form.file_field :pdf %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
